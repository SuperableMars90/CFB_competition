[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: hits the live Aiven database (needs .streamlit/secrets.toml or secrets/aiven.toml and network access). Skip with: pytest -m "not integration"
