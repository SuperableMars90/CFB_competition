# Copy to secrets/aiven.toml and fill in real values.
# DO NOT commit secrets/aiven.toml to git.

[aiven_mysql]
host = "your-project.aivencloud.com"
port = 12345
user = "avnadmin"
password = "your_db_password_here"
database = "defaultdb"
ssl_ca_path = "secrets/ca.pem"
